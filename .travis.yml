language: r
sudo: true
cache: packages

install:
- R -e 'install.packages("jsonlite", "cgdsr", "stringr", "htmlwidgets", "knitr", "rmarkdown", "kableExtra", "shiny")'

script:
- R CMD build .
- R CMD check *tar.gz
